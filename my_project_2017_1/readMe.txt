
이 프로젝트의 목적은 나중에 실험을 할 경우 비교 대상이 필요하다
따라서 이 안에는 기본 형태의 코드를 저장한다

1. MLP_basic : mini-batch, cross-entropy, weight decay (2017.10.28)
2. MLP_adam : mini-batch, cross-entropy, weight decay, adam optimizer (2017.10.30)
3. MLP_regularizaion_v1 :  mini-batch, cross-entropy, weight decay, adam optimizer, max-norm, drop-out (2017.11.4)
4. Graph_structure_v1 : mini-batch, cross-entropy, least-squres, adam & momentum optimizer, max-norm, drop-out (2017.12.4)
                        (maybe) resNet, GANs 
                        (Not yet) Conv structure

5. Graph_structure_v2 : mini-batch, cross-entropy, least-squres, adam & momentum optimizer, max-norm, drop-out (2017.12.4)
                        (maybe) resNet, GANs 
                        (Not yet) Conv structure














